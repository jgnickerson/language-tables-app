var React = require('react');

var Admin = React.createClass({
  render : function() {
    return (
      <div>
        <h2>You've made a reservation for Language Tables on</h2>
      </div>
    );
  }
});

module.exports = Admin;
